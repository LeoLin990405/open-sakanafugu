# End-to-End Workflow Walkthrough

The complete pipeline that takes a requirement from "one sentence" all the way to "reviewed and merged into the main branch."
Four roles, seven phases — fully replayable, interruptible, and auditable.

---

## Roles

| Layer                  | Who                                                                                            | Does                                                                     | Does not do                                                                       |
| ---------------------- | ---------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------- |
| Strategy / Planner     | Human operator or a frontier planning agent                                                    | Write requirements, split tasks, set acceptance criteria                 | Does not enter a worker runtime pane, does not write bulk implementation code     |
| Execution + Supervisor | Any operator agent that can run fugue commands: Claude Code, Codex, OpenCode, or a human shell | Dispatch profiles, integrate, run the quality gate, run tests, log TASKs | Does not hand-write large blocks of implementation except focused Phase 5 patches |
| Implementers           | Agent runtime profiles backed by `fugue-cc`, Codex, OpenCode, or future harnesses              | Write subtasks each in their own worktree or scoped runtime              | Do not read each other's code, do not touch the main branch                       |
| Frontend (opt-in)      | Frontend-capable implementer profile such as Antigravity (`agy` CLI), when policy permits      | Frontend/UI subtasks, manual IDE or headless `agy --prompt`              | **Does not enter the Phase 5 loop; normally never acts as reviewer**              |
| Reviewer               | Independent review profile, usually Codex or another configured reviewer                       | Adversarial review, gives VERDICT + Findings                             | Does not write implementation (keeps generation != review independence)           |

> The maintenance layer **cc-sync** is not on the request path; it is a background launchd daemon: CC upgrade tracking + model refresh + monthly rebuild.

---

## The Seven-Phase Pipeline

### Phase 0 — Open the Task (Planner)

The planner writes the requirement into a task file such as `~/.claude/tasks/TASK-YYYY-MM-DD-NNN.md`: requirements / subtasks (annotated with the logical agent profile) / acceptance criteria / output files. This is the single source of intent for the whole pipeline. `fuguectl task new` uses exclusive file creation for concurrent operators, and all TASK audit appenders (`task log`, `dispatch --task`, `plan --task`, `summary --task`, `integrate --task`) share a lightweight lock with `task done` so overlapping observers preserve their entries even while the task is being closed.
When using the planning panel, pass `fuguectl plan "<goal>" --models m1,m2 --out <dir> --timeout-ms n --allow-partial --codex-clean --harness-arg x --codex-arg x --opencode-arg x --agy-arg x --task <file>` as needed, so planner start/completion status, output size or error kind/exit code, and artifact paths are written into the same audit trail. Use `--harness lite` to ask Codex, OpenCode, and Antigravity for parallel plans from one command; custom lite models are prefixed as `codex:<model>`, `opencode:<provider/model>`, or `agy:<model|default>`. `--codex-clean` applies only to Codex planning targets and keeps the plan output directory writable. `--allow-partial` is for exploratory planning: if at least one successful planner writes an artifact, the command exits 0 while keeping failed planners visible in output and the TASK log. TASK writes are append-safe, so concurrent planning commands preserve each other's audit lines.

### Phase 1 — Split and Assign (fuguectl)

The operator reads the task, splits it into parallelizable subtasks, and picks logical agent profiles by the decision tree:

- Localized docs / provider-specific API / SQL -> matching model profile (doubao/qwen/glm/kimi...)
- English / algorithms / refactoring -> Codex or a strong-reasoning profile (deepseek/minimax)
- Math and logic -> stepfun
- One subtask = one independent, copy-ready prompt (**no broadcasting a single generic prompt to everyone**).
- `fuguectl plan ... --out <dir>` writes `<dir>/summary.json` for automation, with top-level `status`/`exitCode`/`allowPartial`/`succeeded`/`available`/`failed` and per-planner artifact status, duration, and error metadata. It first writes `status=running` with per-planner `artifactStatus=pending`, then atomically replaces that with the final `ok|partial|failed` summary.

### Phase 2 — Parallel Implementation + Cache + join barrier (Implementers)

After the selected runtimes pass preflight (`fuguectl doctor`, `fuguectl preflight --harness <name>`, `fuguectl preflight --harness lite` for Codex + OpenCode + Antigravity, and `fuguectl fleet status` when using the `fugue-cc` worktree fleet), use `fuguectl smoke --harness all --codex-clean --out-dir /tmp/fugunano-smoke` when you need a live lite-runtime proof; the output directory includes per-harness transcripts and `summary.json` for machine parsing with top-level `status`/`passed`/`failed`/`exitCode`, and `--task` records the final summary path plus pass/fail counts:

1. **Open this round's cache**: `fuguectl cache init <round> t1:cc-deepseek t2:cc-glm t3:agy ...` — declare the N tasks dispatched this round (the parallel dispatch manifest).
2. **Guard high-risk prompts**: run `fuguectl guard prompt <prompt-file|-> [--source-ref ref]` before dispatch when the prompt includes browser/email/GitHub material, destructive shell commands, deployment actions, or secrets-adjacent instructions. `BLOCK` exits nonzero; `REVIEW` leaves a deterministic packet for the operator.
3. **Dispatch**: `fuguectl dispatch <agent> --harness fugue-cc|codex|opencode|agy --prompt-file <prompt>` (or `--prompt <text>` for a quick smoke check; add `--verbose` to print timing/output-size observability to stderr; pass `--task <file>` to persist start status plus terminal status, duration, output size, failure kind, and any `--out` artifact path into the audit log) or use an engine `AgentRegistry` so the coordinator resolves the harness from the logical profile; each implementer edits in its own worktree or scoped runtime.
4. **Results land in the cache first**: each agent's output goes to `fuguectl cache put <round> <task_id> <file>` (dead/timed-out -> `fail`, which also counts as "returned"). **Never read from volatile chat/scrollback.**
5. **join barrier (hard gate)**: `fuguectl cache barrier <round> --wait 600` — **if N were dispatched, N must come back** (all terminal) for exit 0; otherwise Phase 3 is not allowed. Stuck tasks surface here and are never silently dropped.

> Logical contract: however many tasks Claude Desktop dispatched, that many must come back before entering the next round. Every round (including each loop of Phase 5) passes this barrier.

### Phase 3 — Integration (fuguectl)

Once the barrier passes (all N returned), the operator pulls outputs from the cache (`fuguectl cache collect <round>`) + cherry-picks each implementer's worktree changes onto the main working branch,
resolves conflicts, unifies style, and runs a local sanity baseline (build/test/lint). `summary --task` and `integrate --task` append Phase 3 audit records, so overlapping operators do not overwrite each other's TASK entries.

### Phase 4 — Review (Reviewer)

`fuguectl dispatch gpt-5.5 --harness codex --prompt-file <review-prompt> --out <verdict-file> --require-output` or a registry-backed reviewer profile gives a `VERDICT` (ACCEPTED / NEEDS FIX) + `Findings`.
Generation != review: implementation and review must resolve to independent model families / runtime paths. Antigravity (`agy`) is supported for implementation; legacy `gemini` CLI is retired.
After a failed review, CI log, timeout, missing artifact, or guard block, run
`fuguectl incident packet <failure-log|-> [--json] [--source-ref ref]` before
learning from the trace or asking Self-Harness to mine weaknesses. It turns raw
failure text into cause-labeled incidents with MAST category, harness layer,
line evidence, source hash, and recovery checks. Then run
`fuguectl incident recovery <failure-log|incident-json|-> [--json]` when the
next operator or agent needs bounded incident-response guidance: containment,
repair, validation, then learning, blocked when no line evidence exists.

### Phase 5 — Review-Fix Loop (bounded closed loop, upgraded per 2026-06 loop engineering research)

Automatically iterate **fix -> re-review** until it passes review, with a capped fallback. See `orchestration/fuguectl/SKILL.md` Phase 5 for details; key points:

1. **Deterministic gate first** — each round runs build/test/lint first (objective pass/fail); red must be fixed, don't waste Codex.
2. **Codex subjective review (incremental)** — from round 2 on, review only this round's diff (saves tokens + stays focused).
3. **keep-best anti-regression** — if a round is worse than the previous one / introduces new problems -> `git reset` back to the best version, discarding the bad change (prevents degeneration of thought).
4. **>=2 confirmation passes** — even after the first ACCEPTED, add one independent confirmation (verification is probabilistic).
5. **Fix = operator patch** (v4 hard rule, no bouncing back to the implementer for a rewrite) + write each round into the TASK file for the audit trail.
6. **Three exit states**: ACCEPTED -> DONE / over MAX_ROUNDS(3) -> escalate to a human / **non-converging -> Meta-Reflector** (first reflect on "why it won't fix" with diagnosis + suggestions, then escalate — not a plain retry).

Research basis: 1-2 rounds capture most reachable improvement, a hard cap prevents oscillation, and generation != review keeps the verifier independent. The memory step follows [Reflexion](https://arxiv.org/abs/2303.11366), [Self-Refine](https://arxiv.org/abs/2303.17651), [Agent Workflow Memory](https://arxiv.org/abs/2409.07429), [AgentHER](https://arxiv.org/abs/2603.21357), [MemRL](https://arxiv.org/abs/2601.03192), and recent stale/evolving/provenance/conflict-aware agent-memory work such as [STALE](https://arxiv.org/abs/2605.06527), [SSGM](https://arxiv.org/abs/2603.11768), [Agent Memory: Characterization and System Implications](https://arxiv.org/abs/2606.06448), [MemMachine](https://arxiv.org/abs/2604.04853), [MemConflict](https://arxiv.org/abs/2605.20926), [Don't Ask the LLM to Track Freshness](https://arxiv.org/abs/2606.01435), [Agent-Native Memory Systems](https://arxiv.org/abs/2606.24775), [Traversal-as-Policy](https://arxiv.org/abs/2603.05517), [MemRefine](https://arxiv.org/abs/2606.13177), [Decision-Aware Memory Cards / CICL](https://arxiv.org/abs/2606.08151), [Useful Memories Become Faulty](https://arxiv.org/abs/2605.12978), [Memory for Autonomous LLM Agents](https://arxiv.org/abs/2603.07670), [Proof-Carrying Agent Actions](https://arxiv.org/abs/2606.04104), [AgentSpec](https://arxiv.org/abs/2503.18666), [AgentVisor](https://arxiv.org/abs/2604.24118), [CaMeL](https://arxiv.org/abs/2503.18813), [HarnessFix](https://arxiv.org/abs/2606.06324), [MAST](https://arxiv.org/abs/2503.13657), [From Agent Traces to Trust](https://arxiv.org/abs/2606.04990), [Debugging the Debuggers / PROBE](https://arxiv.org/abs/2605.08717), [Improving Agent Safety through Incident Response](https://arxiv.org/abs/2602.11749), [Failure-Aware Observability for LLM Agent Systems](https://arxiv.org/abs/2606.01365), [A Taxonomy and Postmortem Analysis of Silent Failures in LLM Applications](https://arxiv.org/abs/2606.14589), [LLM Agents for Interactive Workflow Provenance](https://arxiv.org/abs/2509.13978), [Distilling Feedback into Memory-as-a-Tool](https://arxiv.org/abs/2601.05960), [Structured Belief State](https://arxiv.org/abs/2605.11325), [PROV-AGENT](https://arxiv.org/abs/2508.02866), [Securing LLM-Agent Long-Term Memory Against Poisoning](https://arxiv.org/abs/2606.24322), [From Untrusted Input to Trusted Memory](https://arxiv.org/abs/2606.04329), and [Agents That Know Too Much](https://arxiv.org/abs/2606.26627): useful traces should become reusable verbal feedback, but failed traces must be explicitly relabeled, normalized into provenance-bearing incident packets when possible, converted into evidence-gated recovery packets before repair when the next action is ambiguous, stale traces must be explicitly gated, conflicting traces should be deterministically maintained, imported traces should keep write-time origin metadata before replay, automatic replay should fit bounded prompts without lossy model-side consolidation, action executions should be able to emit portable proof sidecars, high-risk prompts should expose untrusted-input and privileged-action boundaries before runtime execution, and successful traces should be externalized into compact policy/checklist views when full replay is unnecessary. FuguNano's `experience learn --allow-failure --lesson ... --failure-cause <cause> [--supersedes <slug>]` writes that relabel with `source=task:<TASK.md>` provenance; `experience add --source-ref <ref> --trust untrusted [--supersedes <slug>]` marks imported/browser/model-derived notes with a visible origin plus write-time trust; `experience promote <ws> <slug> --source-ref <ref> --confirm-source-ref <ref>` is the explicit origin-bound confirmation gate before an imported memory becomes trusted; `experience recall --failure-cause <cause> --source <manual|task> --source-ref <ref> --trust <trusted|untrusted|all> --min-score <n> --max-age-days <n> [--include-superseded] --explain` filters by failure mode, source class, exact write-time source reference, trust, explicit supersession, utility, and freshness before ranking, and prints retrieval/source/trust/freshness evidence. `experience policy <ws> (<slug>|--query <q>) [--json]` converts exact or recalled memories into provenance-bearing policy cards without model summarization. `workspace context --experience-source <manual|task> [--experience-source-ref <ref>] --experience-limit <n> [--experience-budget-chars <n>] [--experience-trust trusted|all] [--experience-max-age-days <n>]` and `dispatch --workspace ... --experience-source <manual|task> [--experience-source-ref <ref>] --experience-limit <n> [--experience-budget-chars <n>] [--experience-trust trusted|all] [--experience-max-age-days <n>]` apply the same source route, trusted-only default, candidate cap, rendered-character budget, superseded-record hiding, and optional freshness window before automatic prompt assembly, then render each injected method with parse-stable JSON `[experience:meta] {"slug":...,"sourceKind":...,"trustKind":...,"created":...}` plus source-ref/confirmation/failure/supersession metadata when present. `guard prompt <prompt-file|-> [--source-ref ref]` emits an ALLOW/REVIEW/BLOCK runtime guard packet with source hash, line evidence, and recommended checks before dispatch. `incident packet <failure-log|-> [--source-ref ref]` emits a cause-labeled failure packet with MAST category, harness layer, source hash, and line evidence before repair/learning; `incident recovery <failure-log|incident-json|-> [--source-ref ref]` consumes the raw log or packet JSON and emits containment/repair/validation/learning steps, or blocks when line evidence is missing. `dispatch --certificate <file>` applies that evidence-first stance to a single runtime action: it emits a PCAA-style certificate with harness/target, prompt/output hashes, approval class, assumptions, externality facts, and five checkpoints. `task handoff <TASK.md> [--json] [--tail n] [--require-done]` applies the same stance to the TASK artifact itself: it emits acceptance conditions, output objects, checklist state, recent log evidence, local issues, and source provenance before another agent/reviewer/memory learner consumes the work. The source/source-ref/trust/confirmation/supersession/freshness filters, policy-card rendering, whole-record budget packing, runtime guard packets, incident and recovery packets, action certificates, task handoff packets, and injected metadata are operator routing and audit primitives, not complete formal authority boundaries. Harness evolution follows [Self-Harness](https://arxiv.org/abs/2606.09498), [HarnessFix](https://arxiv.org/abs/2606.06324), and related retrospective harness optimization work such as [RHO](https://arxiv.org/abs/2606.05922).

Machine-readable recall is the evaluation handle for that memory loop. `experience recall --json` returns the same filtered results as a stable array with method metadata, source/trust/confirmation/supersession fields, body, score, matched terms, and failure-cause evidence, so operators can inspect recall independently from generated answer quality. `experience eval <ws> --cases <json|jsonl> --json` turns that into a small local benchmark: each case declares expected slugs and optional filters, then the command reports precision, recall, F1, hit, MRR, and aggregate pass counts over the real recall path. `experience audit [workspace] --json [--max-age-days <n>]` checks the store itself before replay: it flags untrusted memories without source refs, trusted imported/manual source-ref memories without confirmation, untrusted replacement claims, missing supersession targets, confirmation/source conflicts, and stale trusted records under an explicit retention window. This follows [Structured Belief State / PrecisionMemBench](https://arxiv.org/abs/2605.11325), file-based [Memory-as-a-Tool](https://arxiv.org/abs/2601.05960), set-level utility framing in [RecoAtlas](https://arxiv.org/abs/2605.18805), [MemoryAgentBench](https://openreview.net/forum?id=DT7JyQC3MR), [StructMemEval](https://arxiv.org/abs/2602.11243), lifecycle/routing modules in [Agent-Native Memory Systems](https://arxiv.org/abs/2606.24775), [Memory Lifecycle Framework / VMG](https://arxiv.org/abs/2604.16548), [Governed Memory](https://arxiv.org/abs/2603.17787), and [MEMFLOW](https://arxiv.org/abs/2603.15125). When a recall JSON audit must cross a trust boundary, `experience recall --json --metadata-only` replaces the raw body with `bodySha256` and `bodyChars`, preserving reproducible recall evidence while reducing the memory-membership leakage risk surfaced by [MRMMIA](https://arxiv.org/abs/2605.27825) and the provenance/evidence-tracing requirements discussed in [From Agent Traces to Trust](https://arxiv.org/abs/2606.04990) and [PROV-AGENT](https://arxiv.org/abs/2508.02866).
sources: [LLM Verification Loops](https://timjwilliams.medium.com/llm-verification-loops-best-practices-and-patterns-07541c854fd8) · [Loop Engineering 2026](https://shaam.blog/articles/loop-engineering-ai-agents) · [Reflexion](https://arxiv.org/abs/2303.11366) · [Self-Refine](https://arxiv.org/abs/2303.17651) · [Agent Workflow Memory](https://arxiv.org/abs/2409.07429) · [AgentHER](https://arxiv.org/abs/2603.21357) · [MemRL](https://arxiv.org/abs/2601.03192) · [How Memory Management Impacts LLM Agents](https://arxiv.org/abs/2505.16067) · [MemConflict](https://arxiv.org/abs/2605.20926) · [Don't Ask the LLM to Track Freshness](https://arxiv.org/abs/2606.01435) · [Agent-Native Memory Systems](https://arxiv.org/abs/2606.24775) · [Traversal-as-Policy](https://arxiv.org/abs/2603.05517) · [MemRefine](https://arxiv.org/abs/2606.13177) · [Decision-Aware Memory Cards / CICL](https://arxiv.org/abs/2606.08151) · [Useful Memories Become Faulty](https://arxiv.org/abs/2605.12978) · [Memory for Autonomous LLM Agents](https://arxiv.org/abs/2603.07670) · [Graph Memory for LLM Agents](https://arxiv.org/abs/2606.06036) · [Externalization in LLM Agents](https://arxiv.org/abs/2604.08224) · [Cost-Sensitive Store Routing](https://arxiv.org/abs/2603.15658) · [Memory Lifecycle Framework / VMG](https://arxiv.org/abs/2604.16548) · [Governed Memory](https://arxiv.org/abs/2603.17787) · [MEMFLOW](https://arxiv.org/abs/2603.15125) · [Proof-Carrying Agent Actions](https://arxiv.org/abs/2606.04104) · [From Agent Traces to Trust](https://arxiv.org/abs/2606.04990) · [PROV-AGENT](https://arxiv.org/abs/2508.02866) · [LLM Agents for Interactive Workflow Provenance](https://arxiv.org/abs/2509.13978) · [Distilling Feedback into Memory-as-a-Tool](https://arxiv.org/abs/2601.05960) · [Structured Belief State](https://arxiv.org/abs/2605.11325) · [Securing LLM-Agent Long-Term Memory Against Poisoning](https://arxiv.org/abs/2606.24322) · [From Untrusted Input to Trusted Memory](https://arxiv.org/abs/2606.04329) / [OpenReview](https://openreview.net/forum?id=5cgg9yenCZ) · [Agents That Know Too Much](https://arxiv.org/abs/2606.26627) · [RecoAtlas](https://arxiv.org/abs/2605.18805) · [Agentic EDA Handoff](https://arxiv.org/abs/2606.19795) · [HarnessFix](https://arxiv.org/abs/2606.06324) · [Self-Harness](https://arxiv.org/abs/2606.09498).

### Phase 6 — Wrap-up (fuguectl)

Review passes -> merge into the main branch, mark the TASK file `DONE`, generate `fuguectl task handoff <TASK.md> --require-done` when the next consumer needs a compact acceptance/evidence packet, keep any `dispatch --certificate` JSON sidecars with high-value runtime actions, clean up worktrees, then run `fuguectl experience learn <workspace> "<retro-title>" --task <TASK.md>` for non-obvious gotchas/decisions so the next workspace context can replay them. If a terminal failed/blocked TASK contains a reusable lesson, learn it only with `--allow-failure --lesson "<relabel>"` and add `--failure-cause <cause>` when the failure mode is clear. After a model, dependency, API, or workflow change, use `--max-age-days` on manual recall or `--experience-max-age-days` on automatic injection before replaying older memories.

---

## Three Ways to Run

|             | Lightweight plugin route                | Worktree fleet route                           | Engine registry route                                          |
| ----------- | --------------------------------------- | ---------------------------------------------- | -------------------------------------------------------------- |
| When to use | One or two subtasks, quick validation   | Real parallel dispatch with isolated worktrees | Mixed runtimes in one typed coordinator round                  |
| Startup     | `/cn:team` `/cn:ask` inside Claude Code | mount provider, then `fuguectl fleet status`   | `wireCoordinator({ agentRegistry })`                           |
| Isolation   | Same process, no worktree               | Each `fugue-cc` profile gets its own worktree  | Per profile: `fugue-cc`, Codex, OpenCode, or future harness    |
| Review      | Manual                                  | Phase 4-5 automatic loop                       | Independent reviewer profile, policy checked by model family   |
| Config      | No provider config needed               | Needs provider config under `.fugue-cc/`       | JSON `AgentRegistry`; see [AGENT_RUNTIME.md](AGENT_RUNTIME.md) |

---

## Maintenance Layer: cc-sync (background launchd)

```bash
cc-sync cli              # Upgrade all envs + the main claude to the latest @anthropic-ai/claude-code
cc-sync models [--apply] # Probe each provider's /v1/models, report/append new models (default profile untouched)
cc-sync research         # agent: read each vendor's official docs -> learn -> rebuild launchers -> live verification
cc-sync all              # cli + models
```

- `WatchPaths` pins the global claude-code `package.json` -> follows upstream the moment it upgrades.
- Monthly `cc-sync research` (launchd `StartCalendarInterval`, the 1st of each month at 05:00) -> doc-driven rebuild.
- **Default/flagship profile changes are always manual** — model fit needs human judgment; automation only "proposes," never "swaps the default."

---

## Security Boundary

- Keys live only in `~/.config/cc-model-secrets.env` (read by the launcher, highest priority); the repo only has a provider config example.
- `.gitignore` ignores `.fugue-cc/` / `**/.fugue-cc/` / `*secrets*.env` / `.env*`; a hard secret scan runs before push, only 0 hits gets pushed.
- Personal paths are generalized into `$FUGUE_CC_WORK` / `$FUGUE_CC_CLAUDE` / `$TASKS` placeholders + the `~/...` convention — substitute for your own environment; a hard secret scan runs before commit, only 0 hits gets pushed.
- Review/second opinion goes through **Codex or another independent reviewer**; do not collapse implementation and review into the same runtime path.
